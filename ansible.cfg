[defaults]
# Avoid ACL chmod failures when becoming an unprivileged user by using world-readable temp files.
allow_world_readable_tmpfiles = True
# Keep temp files on a predictable, writable path on the target.
remote_tmp = /tmp/.ansible-${USER}
